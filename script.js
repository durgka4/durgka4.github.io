const menu=document.querySelector('.menu');const links=document.querySelector('.nav-links');menu?.addEventListener('click',()=>{links.style.display=links.style.display==='flex'?'none':'flex';links.style.position='absolute';links.style.top='72px';links.style.right='14px';links.style.left='14px';links.style.padding='18px';links.style.background='#0a0e15';links.style.border='1px solid #1b2733';links.style.flexDirection='column';links.style.gap='16px'});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>{if(innerWidth<901)links.style.display='none'}));
const rate=document.getElementById('trafficRate');const line=document.getElementById('trafficLine');let t=0;setInterval(()=>{t+=.7;const value=(1.12+Math.abs(Math.sin(t))*0.72+Math.random()*.08).toFixed(2);if(rate)rate.textContent=value+' Tbps';if(line){const pts=Array.from({length:21},(_,i)=>`${i*40},${Math.max(35,205-Math.abs(Math.sin(t+i*.55))*150-Math.random()*35)}`);line.setAttribute('points',pts.join(' '));}},900);
